"""
Una tupla es una secuencia inmutable de elementos.Significa que una vez creada, no se puede modificar
Esto lo hace útil para almacenar datos que no se espera que cambien.
"""
tupla = (1, 2, 3, 4, 5)

tupla = tuple([1, 2, 3, 4, 5])

tupla = 1, 2, 3, 4, 5

print(tupla)
